# Supabase CLI configuration for JobNomad
# Run: supabase link --project-ref <your-project-ref>
# Apply migrations: supabase db push
# Generate types: supabase gen types typescript --linked > src/lib/supabase/database.types.ts

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 17

[studio]
enabled = true
port = 54323

[inbucket]
enabled = true
port = 54324

[storage]
enabled = false
# Not used in phase 1

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = []
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10
enable_signup = true
enable_anonymous_sign_ins = false

[auth.email]
enable_signup = true
double_confirm_changes = true
enable_confirmations = true
# Magic link: token expires after 1 hour (phase 1: magic links only)

[auth.sms]
enable_signup = false
enable_confirmations = false

[analytics]
enabled = false
