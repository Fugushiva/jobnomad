# =============================================================================
# JobNomad — Variables d'environnement
# =============================================================================
# Copier ce fichier en .env.local et remplir les valeurs.
# NE JAMAIS committer .env.local (gitignored).
#
# Convention de nommage Supabase :
#   NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY = anciennement "anon key"
#   (Supabase a renommé la clé anonyme en "publishable key" fin 2024)
#   Les variables NEXT_PUBLIC_* sont exposées au navigateur — n'y mettre
#   que des valeurs PUBLIQUES (URL + clé publishable).
# =============================================================================

# App — URL publique du site
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase — Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
# Anciennement "anon key" — visible dans le navigateur, sans danger
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# Supabase — Project Settings > API > service_role
# ⚠️  SERVEUR UNIQUEMENT — ne jamais exposer côté client
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Stripe — Dashboard > Developers > API keys
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Resend — resend.com > API Keys
RESEND_API_KEY=re_...

# Google AI — aistudio.google.com
GEMINI_API_KEY=AIza...

# OpenAI — platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...

# Vercel Cron — doit correspondre à l'en-tête Authorization: Bearer <secret>
# Générer avec : openssl rand -hex 32
CRON_SECRET=<chaine-aleatoire-32-chars>

# Rate limiting — pepper pour hasher les IPs (stocké en DB, jamais en clair)
# Générer avec : openssl rand -hex 16
RATE_LIMIT_PEPPER=<chaine-aleatoire-16-chars>
